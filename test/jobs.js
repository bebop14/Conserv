/**
 * Created by naver on 15. 7. 22..
 */
var	chai = require('chai'),
		expect = chai.expect;

describe("Route", function () {
	describe('Jobs test', function () {
		it('GET / should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('GET /jobs should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('PUT /jobs should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('POST /jobs should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('DELETE /jobs should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
	});
});
