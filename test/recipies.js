/**
 * Created by naver on 15. 7. 22..
 */
var	chai = require('chai'),
		expect = chai.expect;

describe("Route", function () {
	describe('Recipies test', function () {
		it('GET /recipe should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('GET /recipies should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('PUT /recipe should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('POST /recipe should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('DELETE /recipe should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
	});
});
