/**
 * Created by naver on 15. 7. 22..
 */
var	chai = require('chai'),
		expect = chai.expect;

describe("Route", function () {
	describe('Repos test', function () {
		it('GET / should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('GET /repos should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('PUT /repos should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('POST /repos should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
		it('DELETE /repos should succeed with response 200', function (done) {
			setTimeout(done, 1000);
		});
	});
});
